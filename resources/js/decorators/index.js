const withAddingNote = require('./add-note-decorator');
const withListPagination = require('./list-pagination-decorator');
const withSpeech = require('./speach-decorator');

module.exports = {
    withAddingNote,
    withListPagination,
    withSpeech
};
